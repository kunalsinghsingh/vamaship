$(document).ready(function (){    $(document).on("click", ".status_approve", function () {        var id = ($(this).attr('id'));        var text = ($.trim($(this).text()));        if (text == "Enable")        {            var status_type = 1;        }        else if (text == "Disable")        {            var status_type = 0;        }        var finaldata = "status=" + status_type + "&res_id=" + id;        $("#" + id).text(text == "Disable" ? "Enable" : "Disable");        $.ajax({            type: "POST",            url: SITE_URL + "admin/restaurant/booking/update_status",            dataType: "html",            data: finaldata,            success: function (msg)            {            }        });    });});